def greedy_select(test_cases, budget, score_fn, data):
    # TODO
    return None


def prioritize_total_coverage(test_cases, budget, coverage_df):
    # TODO
    return None


def prioritize_diff_coverage(test_cases, budget, coverage_df, changed_lines: set[str]):
    # TODO
    return None


def prioritize_fault_detection(test_cases, budget, failure_rates: list[float]):
    # TODO
    return None


def prioritize_coverage_per_cost(test_cases, budget, coverage_df, exec_times: list[float]):
    # TODO
    return None
